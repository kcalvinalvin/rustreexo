// Rustreexo

/*
/// transform is the function used for re-organzing Utreexo tree. Given a vector
/// of positions to be deleted, it returns a
fn transform(dels: Vec<u64>, num_leaves: u64, forest_rows: u8) -> Vec<Vec<arrow>> {
    let num_next_leaves = num_leaves - len(dels);

    for r in 0..rows {
        delRoot();
        let pair = extract_twins(dels, forestRows);
        swapNextDels();
    }
    return swaps;
}
*/
